<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:locale="pt-BR"
    tools:showIn="@layout/fragment_produto_list">

    <data>

        <variable
            name="produto"
            type="tk.andrielson.carrinhos.androidapp.data.model.Produto" />

        <variable
            name="handler"
            type="tk.andrielson.carrinhos.androidapp.ui.viewhandler.ProdutoHandler" />

        <import type="java.util.Locale" />
    </data>

    <LinearLayout
        android:id="@+id/fragment_produto_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@color/colorAccent"
        android:onClick="@{handler::onClickProduto}"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/fragment_produto_nome"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:layout_weight="3"
            android:text="@{produto.nome}"
            android:textAppearance="?attr/textAppearanceListItem"
            android:textStyle="bold"
            tools:text="Picolé de leite" />

        <TextView
            android:id="@+id/fragment_produto_valor"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:layout_weight="1"
            android:text='@{String.format(Locale.getDefault(), "R$ %.2f", produto.getPreco())}'
            android:textAppearance="?attr/textAppearanceListItem"
            tools:text="R$ 2,50" />
    </LinearLayout>
</layout>